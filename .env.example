# Session signing secret for the passphrase-based tester login (NOT-NOW.md item 2).
# Generate one with: openssl rand -hex 32
SESSION_SECRET=

# Comma-separated allowlist of tester passphrases, one per tester.
# The app never records which passphrase belongs to which person.
AUTH_TESTER_PASSPHRASES=
